define(['knockout', 'underscore'], function(ko, _) {
  'use strict';

  // Create state object (singleton)
  var state = {
  };

  // Return state object (singleton)
  window.state = state;
  return state;
});